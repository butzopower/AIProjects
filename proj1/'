class Row
  attr_accessor :player, :houses

  def initialize(houses, player)
    self.houses = houses
    self.player = player
  end

  # initial call to start moving pieces
  def apply(position)
    remaining = houses[position] # get the number of pieces
    houses[position] = 0
    (position + 1).upto(5) {|x| 
  end

  def apply_moves_from_player(player, 

end
